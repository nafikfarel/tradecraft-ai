version: '3.8'

services:
  tradecraft-ai:
    build: .
    container_name: tradecraft-ai-pro
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
